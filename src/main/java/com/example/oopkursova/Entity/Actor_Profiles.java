package com.example.oopkursova.Entity;

public class Actor_Profiles {
    // создать новую сущность
    // для актеров , куда они будут добавлять в свои профили
    // свои возможности и умения , по тибу своего личного кабинета для актера
    // вот пример кода скл
    // CREATE TABLE actor_profiles (
    //    id BIGSERIAL PRIMARY KEY,
    //    actor_id BIGINT UNIQUE NOT NULL,
    //    bio TEXT,
    //    skills TEXT, -- JSON або CSV (наприклад, "Фехтування, Танці, Вокал")
    //    experience TEXT, -- Опис досвіду
    //    profile_photo_url VARCHAR(500), -- Посилання на фото
    //    FOREIGN KEY (actor_id) REFERENCES users(user_id)
    //);
}
